<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EspinhoAI.HomePage"
             Title="Home">
    <Grid x:Name="myLayout">
        <Grid.GestureRecognizers>
            <DropGestureRecognizer AllowDrop="True" DragOver="DropGestureDragOver" Drop="DropGestureDrop" DragLeave="DropGestureDragLeave"/>
        </Grid.GestureRecognizers>
        <Border Margin="20" Padding="20" StrokeThickness="1" x:Name="border">

            <Label x:Name="lblPath" HorizontalOptions="Center" VerticalOptions="Center" FontSize="Header">
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="Drop your pdf here, or " FontAttributes="Bold"/>
                        <Span Text="browse here" TextColor="{StaticResource Primary}" FontAttributes="Bold" >
                            <Span.GestureRecognizers>
                                <TapGestureRecognizer  Tapped="TapGestureRecognizer_Tapped" />
                            </Span.GestureRecognizers>
                        </Span>
                    </FormattedString>
                </Label.FormattedText>
            </Label>
        </Border>

    </Grid>
</ContentPage>